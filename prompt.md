# 思源学堂课程资料下载 —— 可复用 Prompt

把下面这段直接贴进对话（改掉 `【】` 里的两处）即可。已装 `xjtu-lms-grab` skill 时，
前半段会自动被 skill 接住；没装 skill 时这段话本身也足够让 AI 照做。

---

帮我下载这门课的资料，课程链接：【https://lms.xjtu.edu.cn/course/<课程ID>/index】，
保存到：【<本地输出目录>】。

只处理这个账号有权访问的课程资源，不要尝试访问其他课程。

要求：

1. 用 Playwright 的持久化 profile 启动浏览器（headless=False），打开课程页，我手动登录一次；
   登录成功后把登录态存成 JSON，之后所有下载都用 cookie 直连接口，不要再开浏览器。
2. 先干跑输出一份附件清单给我确认（文件名 + 大小 + 所属活动），我点头再真下。
3. 下载要覆盖两类来源，**缺一不可**：
   - 活动数据里的附件数组；
   - `type=page` 活动正文里内嵌的附件链接 —— 课件 PDF 基本都藏在这里，
     只看附件字段会误判成「这门课没有课件文件」。
4. 文件名取附件元信息接口返回的 `name`，不要从正文链接文字猜（两者常不一致）。
5. 幂等：已存在的文件跳过，可反复跑做增量补件。
6. 取不到元信息的（403/404）标记为不可用并跳过，不要反复重试。
7. 产物按 `<目录>/{课件,作业}/<活动标题>/<文件名>` 组织。
8. 脚本里不要写死任何本机专属路径：Python 用系统默认、浏览器自动探测
   （Edge → Chrome → 自带 chromium）、登录态默认放 `~/.lms-grab/`，都可用环境变量覆盖。
9. 请求保持串行，不要并发；遇到权限类错误立即停止，不要重试。

下载完成后，先给我一份「文件名 → 所属章节/活动 → 大小」的清单，
再问我要不要进一步整理成按章分文件夹的结构（不要自作主张搬动我已有的文件夹）。

---

## 常用变体

只补新作业，不要旧的：

> 同上，但跳过文件名里含 2020/2021/2022 的旧版作业 PDF，只保留最新版。

把项目压缩包单独拿出来：

> 同上，但把 zip 项目包单独放进 `项目/`，不要混在作业目录里。

换一门课：改 `--course` 的课程 ID 和输出目录即可，登录态按课程 ID 分开存。
